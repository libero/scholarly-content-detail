version: "3.4"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /srv/app/
    command: /bin/bash -c "python manage.py migrate && python manage.py runserver 0:8000"
    volumes:
      - ./:/srv/app/
    ports:
      - 8000:8000
